const input = prompt ('Введіть значення');

function padString (str, length, symbol, toLeft = false) {
    if (str.length <= length) {
        return str;
    }
    
}

console.log(padString(input));
